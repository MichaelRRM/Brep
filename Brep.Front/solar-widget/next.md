-there should be a spinner on every api call => done 

-when the intraday chart is updated it seems to be rendered twice for each api call (I see the animation twice) 

-when on daily, on page reload, the year and month in the selector on the right are the not the one selected by default 

